<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:redeemer.beginning="http://schemas.android.com/apk/res-auto" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_search"
        android:icon="@drawable/ic_action_search"
        android:title="@string/action_search"
        redeemer.beginning:showAsAction="ifRoom" />
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_add_field"
        android:title="@string/action_add_field"
        redeemer.beginning:showAsAction="never" />
    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        redeemer.beginning:showAsAction="never" />
</menu>